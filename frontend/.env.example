# Example frontend environment variables
# Copy to .env.local and customize. Do NOT commit real secrets.

# Must match backend AUTH_SECRET exactly (frontend verifies backend-issued session JWT).
AUTH_SECRET=replace-with-a-strong-random-string
BACKEND_URL=http://localhost:8080
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# OIDC variables were removed from frontend.
# Backend is the only OIDC client and handles /auth/login + /auth/callback.
