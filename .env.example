# Backend database config - populate with your Mongo connection string
MONGO_URI=mongodb://mongo:27017
DB_NAME=records_db
ADMIN_USER=admin
ADMIN_PASS=admin123
AUTH_SECRET=change_me_to_a_secure_random_value
# When running under Docker Compose where frontend and backend share the same network,
# use the backend service name. This allows the frontend container to reach the backend:
NEXT_PUBLIC_BACKEND_URL=http://backend:8080

# If you want the browser on your host machine to call the backend at http://localhost:8080,
# add a ports mapping for the backend in docker-compose.yml: "8080:8080" and keep the value
# as http://localhost:8080 instead.