# Example environment variables for docker-compose (copy to .env and edit)
# Frontend doesn't need special envs by default but kept here for convenience
PORT=3000

# Backend database config - populate with your Mongo connection string
MONGO_URI=mongodb://mongo:27017
DB_NAME=records_db
ADMIN_USER=admin
ADMIN_PASS=admin123
AUTH_SECRET=change_me_to_a_secure_random_value
